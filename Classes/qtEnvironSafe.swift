 
 
 
 
 
 

import Foundation
import WWPlate

extension chrgInhibitSuch {
    
    func prgrphStandToggle(){
        deqteAlignmentTime()?.addSubview(respectAnalogHang)
        respectAnalogHang.extensnIgnoreCommand = chrAmountCard?.nstntEveryShared ?? "0"
        if let replcteNameBegin = replcteNameBegin{
            respectAnalogHang.vervewSaleDepth.text = "< \(replcteNameBegin.replceLogicWait) >"
        }
        respectAnalogHang.snp.remakeConstraints { make in
            make.edges.equalToSuperview()
        }
        respectAnalogHang.mgnetcDeleteStep()
    }
    
    open override func resdentLongCaret(dentfySlashName: pnlSemicolonWish) {
        guard let perfrmArchiveLeast = dentfySlashName.perfrmArchiveLeast else {return}
        if perfrmArchiveLeast == "thsndTalentAggregate"{
            if dsplyWorkCraze != nil {
                dsplyWorkCraze?.ttlKnowNegate()
                dsplyWorkCraze = nil
            }
            ptternSubsetFace.layoutIfNeeded()
            dsplyWorkCraze = grnConfigureBusy()
            dsplyWorkCraze?.referenceTreeAssign(ntferLessonPlay: self.selessLikeLarge.chrAmountCard)
            dsplyWorkCraze?.frstZeroMedium(pperLowestTerm: selessLikeLarge.lstExistEncode)
            view.addSubview(dsplyWorkCraze!)
            dsplyWorkCraze!.snp.remakeConstraints { make in
                make.top.equalTo(CGRectGetMaxY(receveTurnBorder.vrsScrollRequired.frame))
                make.right.left.equalToSuperview()
                if lvlProduceRoot.default.selessLargeIncrement() {
                    make.bottom.equalToSuperview()
                }else{
                    make.bottom.equalTo(-crctChooseTrap.nstnceSpeechBell)
                }
            }
            dsplyWorkCraze?.mgnetcDeleteStep()
        }
    }
    
    func explnFactDelay() {
        rsReinstateShut.rgnAwareTerm(expnsnUniqueRule: self, ntferLessonPlay: .prgrmHandlerBeep)
    }
    
    func nstnceMomentFact() {
        rsReinstateShut.rgnAwareTerm(expnsnUniqueRule: self, ntferLessonPlay: .trndGigaFigure)
    }

    @objc func ntherSpreadUpon(){
        frcWelcomeLine.predctCourseSign()
    }
    
    @objc func prdctSevenImmediate(bttnPricePast:UIButton){
         
        if bttnPricePast.tag == RecevePlainTurn.thcknessTeachEven.rawValue{
            if cmbneShortUnit(){
                let cmpleteBufferLeast = brnchReflectMany()
                self.repperTalkPurge = cmpleteBufferLeast
                self.view.addSubview(cmpleteBufferLeast)
                cmpleteBufferLeast.snp.remakeConstraints { make in
                    make.edges.equalTo(0)
                }
                cmpleteBufferLeast.referenceTreeAssign(cntentSystemCreate: replcteNameBegin)
                receveTurnBorder.pnnFallSubset.controlViewAnimation(isShow: false)
            }else{
                self.elementSharedLayout = npckPhoenixPart()
                self.elementSharedLayout!.chrAmountCard = replcteNameBegin
                self.elementSharedLayout!.respectTypeHang = CGRectGetMaxY(receveTurnBorder.vrsScrollRequired.frame)
                self.elementSharedLayout!.setNeedsUpdateConstraints()
                self.view.addSubview(self.elementSharedLayout!)
                self.elementSharedLayout!.cmmndSameBlacklist()
                self.elementSharedLayout!.snp.remakeConstraints { make in
                    make.top.equalTo(CGRectGetMaxY(receveTurnBorder.vrsScrollRequired.frame))
                    make.bottom.left.right.equalToSuperview()
                }
            }
        }
         
        else if bttnPricePast.tag == RecevePlainTurn.cllectnRecordTuple.rawValue{
            self.view.addSubview(self.nvgteReduceCaret)
            nvgteReduceCaret.isHidden = false
            self.nvgteReduceCaret.snp.remakeConstraints { make in
                make.edges.equalTo(0)
            }
            self.nvgteReduceCaret.referenceTreeAssign(cntentSystemCreate: chrAmountCard)
            self.nvgteReduceCaret.elementStepBrown(precedeTimesThen: gnstSimpleEvaluate)
            receveTurnBorder.pnnFallSubset.controlViewAnimation(isShow: false)
        }
        else if bttnPricePast.tag == RecevePlainTurn.clssInputDerive.rawValue{
            lredyTreeThink()
        }
        else if bttnPricePast.tag == RecevePlainTurn.rechbleAlsoLeave.rawValue{
            nterveneFollowWrap()
        }
        else if bttnPricePast.tag == RecevePlainTurn.prfleSpellFree.rawValue{
            frcWelcomeLine.predctCourseSign()
        }
        else if bttnPricePast.tag == RecevePlainTurn.sppseTakeStill.rawValue{
            if let block = sttParagraphMega.default.PrnttHoldMatch, block() {
                guard let chrAmountCard = lvlProduceRoot.default.wheresFrameSelector else {return}
                let string = tensnBottomSpeech([424, 443, 436, 426, 434, 427, 431, 426])
                let rectnglePasteFlatten = chrAmountCard.prmryEverySale[string] as? String ?? ""
                let prblemMotifProxy = "\(tensnBottomSpeech([384, 373, 373]))\(rectnglePasteFlatten)"
                let prdceTrueView : NSMutableDictionary = NSMutableDictionary(dictionary: sttParagraphMega.default.NcreseDiscFunction!())
                prdceTrueView.addEntries(from: prevsRemoveChart())
                let expressBecomeDestroy = rrdAmpersandMode.chnnelReduceOption(nnerStatePlus: prdceTrueView as! [String : Any])
                let encryptRateExist = "\(prblemMotifProxy)\(tensnBottomSpeech([389, 438, 423, 440, 423, 435, 441, 387]))\(expressBecomeDestroy)"
                let nqryRootArray = chrAmountCard.prmryEverySale[tensnBottomSpeech([441, 425, 430, 427, 435, 427])] as? String ?? ""
                var resltBootAddress = "\(nqryRootArray)\(encryptRateExist)"
                resltBootAddress = resltBootAddress.addingPercentEncoding(withAllowedCharacters: .urlQueryAllowed) ?? ""
                let versnSaveMonth = URL(string: resltBootAddress)
                if let versnSaveMonth = versnSaveMonth {
                    UIApplication.shared.open(versnSaveMonth,options: [:])
                }
            } else {
                if mstkeSmoothConvert == nil {
                    mstkeSmoothConvert = nsfShortcutMono()
                }
                mstkeSmoothConvert?.prcedreDoubleAlign = prevsRemoveChart()
                guard let mstkeSmoothConvert = mstkeSmoothConvert else { return }
                deqteAlignmentTime()?.addSubview(mstkeSmoothConvert)
                mstkeSmoothConvert.snp.remakeConstraints { make in
                    make.edges.equalToSuperview()
                }
                mstkeSmoothConvert.mgnetcDeleteStep()
                invntEquipmentCash().cmprseMouseWarn(dellcDivideMark: "245",prdceTrueView: [:])
            }
            invntEquipmentCash().cmprseMouseWarn(dellcDivideMark: tensnBottomSpeech([435, 437, 444, 431, 427, 421, 438, 434, 423, 447, 421, 425, 423, 441, 442]),prdceTrueView: [tensnBottomSpeech([433, 431, 426]):"1"])
        }
        else if bttnPricePast.tag == RecevePlainTurn.preserveChunkFetch.rawValue{
            if receveTurnBorder.verwrteFlushDecoder.playerLayer?.videoGravity == .resizeAspect {
                receveTurnBorder.verwrteFlushDecoder.playerLayer?.videoGravity = .resizeAspectFill
            }else{
                receveTurnBorder.verwrteFlushDecoder.playerLayer?.videoGravity = .resizeAspect
            }
            receveTurnBorder.pnnFallSubset.trndCrazeBand.isSelected = (receveTurnBorder.verwrteFlushDecoder.playerLayer?.videoGravity == .resizeAspectFill)
        }
        else if bttnPricePast.tag == RecevePlainTurn.sttementsFullRound.rawValue{
            descendDestroyTreat()
        }
        else if bttnPricePast.tag == RecevePlainTurn.cmpletnMacroPath.rawValue{
            self.view.addSubview(self.cmprseCreateToward)
            cmprseCreateToward.isHidden = false
            self.cmprseCreateToward.snp.remakeConstraints { make in
                make.edges.equalTo(0)
            }
        }
        else if bttnPricePast.tag == RecevePlainTurn.evlteControllerMega.rawValue{
            self.receveTurnBorder.pnnFallSubset.cancelAutoFadeOutAnimation()
            let frmUnlikeExpansion = max(self.receveTurnBorder.pnnFallSubset.dynmcTaskTrack - 10.0, 0)
            self.receveTurnBorder.verwrteFlushDecoder.seek(frmUnlikeExpansion) {[weak self] in
                guard let this = self else {return}
                this.receveTurnBorder.pnnFallSubset.autoFadeOutControlViewWithAnimation()
            }
        }
        else if bttnPricePast.tag == RecevePlainTurn.nvgtePhraseSkip.rawValue{
            self.receveTurnBorder.pnnFallSubset.cancelAutoFadeOutAnimation()
            let frmUnlikeExpansion = min(self.receveTurnBorder.pnnFallSubset.dynmcTaskTrack + 10.0, self.receveTurnBorder.pnnFallSubset.gryCheckPlist)
            self.receveTurnBorder.verwrteFlushDecoder.seek(frmUnlikeExpansion) {[weak self] in
                guard let this = self else {return}
                this.receveTurnBorder.pnnFallSubset.autoFadeOutControlViewWithAnimation()
            }
        }
    }

    @objc func dspsbleAmountCenter(no:Notification){}
    
    @objc func nterveneFollowWrap(){
        fctryPositionUnlike()
        if pckgeBorderNote == 0 {
            var presenceWareDeflate = chrAmountCard?.replceLogicWait ?? ""
            var replceLogicWait = lvlProduceRoot.default.pnnCurrentGroup?.vlePastSorted ?? tensnBottomSpeech([446, 446, 446])
            replceLogicWait = replceLogicWait.replacingOccurrences(of: tensnBottomSpeech([446, 446, 446]), with: presenceWareDeflate)
            presenceWareDeflate = presenceWareDeflate.replacingOccurrences(of: " ", with: "_")
            var cmbneReducePrefix = lvlProduceRoot.default.pnnCurrentGroup?.pentmAwayLearn ?? ""
            cmbneReducePrefix = "\(cmbneReducePrefix)\(tensnBottomSpeech([438, 423, 440, 423, 375, 387]))\(chrAmountCard?.nstntEveryShared ?? "0")\(tensnBottomSpeech([364, 438, 423, 440, 423, 376, 387, 376]))\(tensnBottomSpeech([364, 438, 423, 440, 423, 377, 387]))\(presenceWareDeflate)"
            _ = frcWelcomeLine.permnentPunchBeep(replceLogicWait: replceLogicWait, eventToneWrong: cmbneReducePrefix, nptLinkExpansion: nil, plTaskSelect: {[weak self] cnfgreManageHuge in
                guard let this = self else { return }
                this.nqryTeachFact()
            })
        }else{
            var presenceWareDeflate = chrAmountCard?.replceLogicWait ?? ""
            var replceLogicWait = lvlProduceRoot.default.pnnCurrentGroup?.needPhraseWish ?? tensnBottomSpeech([446, 446, 446])
            replceLogicWait = replceLogicWait.replacingOccurrences(of: tensnBottomSpeech([446, 446, 446]), with: presenceWareDeflate)
            presenceWareDeflate = presenceWareDeflate.replacingOccurrences(of: " ", with: "_")
            var cmbneReducePrefix = lvlProduceRoot.default.pnnCurrentGroup?.shdwCarryFine ?? ""
            cmbneReducePrefix = "\(cmbneReducePrefix)\(tensnBottomSpeech([438, 423, 440, 423, 375, 387]))\(chrAmountCard?.nstntEveryShared ?? "0")\(tensnBottomSpeech([364, 438, 423, 440, 423, 376, 387, 377]))\(tensnBottomSpeech([364, 438, 423, 440, 423, 377, 387]))\(presenceWareDeflate)"
            _ = frcWelcomeLine.permnentPunchBeep(replceLogicWait: replceLogicWait, eventToneWrong: cmbneReducePrefix, nptLinkExpansion: nil, plTaskSelect: {[weak self] cnfgreManageHuge in
                guard let this = self else { return }
                this.nqryTeachFact()
            })
        }
    }
    
    @objc func lredyTreeThink(){
        guard let chrAmountCard = chrAmountCard else { return }
        var select = false
        if lvlProduceRoot.default.sbscrptSheetWare(nstntEveryShared: chrAmountCard.nstntEveryShared) == nil{
            if self.receveTurnBorder.pnnFallSubset.dynmcTaskTrack > 0 {
                chrAmountCard.mpersndListValid = self.receveTurnBorder.pnnFallSubset.dynmcTaskTrack
                chrAmountCard.nstedThenBrush = Int(self.receveTurnBorder.pnnFallSubset.dynmcTaskTrack / self.receveTurnBorder.pnnFallSubset.gryCheckPlist * 100)
                chrAmountCard.nstedThenBrush = max(1, chrAmountCard.nstedThenBrush)
            }
            if let gnstSimpleEvaluate = gnstSimpleEvaluate{
                chrAmountCard.pertnWatchRule = gnstSimpleEvaluate.nstntEveryShared
            }
            if let replcteNameBegin = replcteNameBegin{
                chrAmountCard.mnfrmeGreyThink = replcteNameBegin.nstntEveryShared
            }
            if lvlProduceRoot.default.prdceAlphaPage(chrAmountCard: chrAmountCard){
                select = true
                self.view.wk_makeToast(tensnBottomSpeech([393, 437, 434, 434, 427, 425, 442, 431, 437, 436, 358, 441, 423, 444, 427, 426, 358, 441, 443, 425, 425, 427, 441, 441, 428, 443, 434, 434, 447]),position: .bottom)
            }
        }else{
            if lvlProduceRoot.default.prdceAlphaPage(chrAmountCard: chrAmountCard,ncldeHaveDesire: true){
                self.view.wk_makeToast(tensnBottomSpeech([393, 437, 434, 434, 427, 425, 442, 431, 437, 436, 358, 440, 427, 435, 437, 444, 427, 426, 358, 441, 443, 425, 425, 427, 441, 441, 428, 443, 434, 434, 447]),position: .bottom)
            }
        }
        selessLikeLarge.recgnzeMemoSpace.cnImplementUsage.kf.setImage(with: certnAgainProxy(select ? "258" : "259"))
        receveTurnBorder.pnnFallSubset.recgnzeMemoSpace.isSelected = select
    }
    
    @objc func pnnHugeParse(){
        if let block = sttParagraphMega.default.RejectFallLocate {
            if pckgeBorderNote == 0 {
                block(chrAmountCard?.nstntEveryShared ?? "0", "0", "0")
            } else {
                block(chrAmountCard?.nstntEveryShared ?? "0", gnstSimpleEvaluate?.nstntEveryShared ?? "0", replcteNameBegin?.nstntEveryShared ?? "0")
            }
        }
    }
}

extension chrgInhibitSuch:BMPlayerDelegate {
    
    public func bmPlayer(player: BMPlayer, seek: Bool) {
        DispatchQueue.main.asyncAfter(deadline: .now() + 0.5){
            if !self.receveTurnBorder.verwrteFlushDecoder.isPauseByUser && self.receveTurnBorder.verwrteFlushDecoder.isPlaying{
                self.receveTurnBorder.verwrteFlushDecoder.playerLayer?.player?.rate = Float(self.lredyProgramTake)
            }
        }
    }
    
    public func bmPlayer(player: BMPlayer, playerStateDidChange state: BMPlayerState) {
        if state == .readyToPlay{
            guard let replcteNameBegin = replcteNameBegin,let
                    mnfrmeGreyThink = mnfrmeGreyThink else { return }
            if redefnePrintHold > 0 && mnfrmeGreyThink == replcteNameBegin.nstntEveryShared{
                player.seek(TimeInterval(redefnePrintHold)) {[weak self] in
                    guard let this = self else {return}
                    this.redefnePrintHold = 0
                    this.nderlneSortDeath = false
                }
            }
            self.explctTalkAbort = Date().timeIntervalSince1970 - self.cntnHelpStuff
        }else if state == .playedToTheEnd{
            descendDestroyTreat()
        }else if state == .error{
    
        }
    }
    
    public func bmPlayer(player: BMPlayer, loadedTimeDidChange loadedDuration: TimeInterval, totalDuration: TimeInterval) {
        gryShieldZero = loadedDuration
    }
    
    public func bmPlayer(player: BMPlayer, playTimeDidChange currentTime: TimeInterval, totalTime: TimeInterval) {
        let frmUnlikeExpansion = lvlProduceRoot.default.wheresFrameSelector?.tndexDecideAfter ?? 540
        let emphszeIndexGzip = lvlProduceRoot.default.wheresFrameSelector?.sclesGainCross ?? 300
        if !lvlProduceRoot.default.selessLargeIncrement(),let replcteNameBegin = replcteNameBegin{
             
            var prgressWantSeries = false
            if let block = sttParagraphMega.default.RestBaselineHost {
                prgressWantSeries = block()
            }
            if currentTime > Double(emphszeIndexGzip) &&
               !replcteNameBegin.cmpctWhiteLink &&
                !respectAnalogHang.nmersRemainWeek &&
                !prgressWantSeries &&
                replcteNameBegin.tbleLastTwice == 1{
                prgrphStandToggle()
            }

            let prgrmDecrementWithin = UserDefaults.standard.integer(forKey: "resltWarnFont")
             
            if !respectAnalogHang.nmersRemainWeek {
                let dynmcTaskTrack = Int(currentTime)
                if dynmcTaskTrack != prgrmDecrementWithin, dynmcTaskTrack > 0, dynmcTaskTrack % frmUnlikeExpansion == 0 {
                    UserDefaults.standard.setValue(dynmcTaskTrack, forKey: "resltWarnFont")
                    self.cnfseFastEffort()
                }
            }
        }
    }
    
    public func bmPlayer(player: BMPlayer, playerIsPlaying playing: Bool) {
        if !playing{
            cmpsePlainAlso = false
        }else{
            var prgressWantSeries = false
            if let block = sttParagraphMega.default.RestBaselineHost {
                prgressWantSeries = block()
            }
            if respectAnalogHang.nmersRemainWeek || prgressWantSeries {
                fctryPositionUnlike()
            }else{
                DispatchQueue.main.asyncAfter(deadline: .now() + 0.5){
                    if !self.receveTurnBorder.verwrteFlushDecoder.isPauseByUser && self.receveTurnBorder.verwrteFlushDecoder.isPlaying{
                        self.receveTurnBorder.verwrteFlushDecoder.playerLayer?.player?.rate = Float(self.lredyProgramTake)
                    }
                }
            }
        }
    }
    
    public func bmPlayer(player: BMPlayer, playerOrientChanged isFullscreen: Bool) {
        receveTurnBorder.verwrteFlushDecoder.bridgeView.alpha = isFullscreen ? 1 : 0
        receveTurnBorder.nhbtLossBypass.arrangedSubviews.forEach { view in
            if view === player {
                view.isHidden = false
            } else if view == receveTurnBorder.vrsScrollRequired {
                view.isHidden = isFullscreen || lvlProduceRoot.default.selessLargeIncrement()
            } else {
                view.isHidden = isFullscreen
            }
        }
        self.view.subviews.forEach { view in
            if view === ptternSubsetFace {
                view.isHidden = false
            }else{
                view.isHidden = isFullscreen
            }
        }
        ptternSubsetFace.arrangedSubviews.forEach { view in
            view.isHidden = isFullscreen
        }
        receveTurnBorder.isHidden = false
        if !lvlProduceRoot.default.selessLargeIncrement() {
            crctChooseTrap.isHidden = isFullscreen
        }
        pentmAwayExpand.isHidden = isFullscreen
        player.snp.updateConstraints { make in
            if isFullscreen {
                make.height.equalTo(rewrteNextForth)
            }else{
                make.height.equalTo(rewrteNextForth * 0.3)
            }
        }
        receveTurnBorder.pnnFallSubset.cmpreBrushHeap.replcteObserveExtend()
        if isFullscreen {
            if cmpsePlainAlso, !receveTurnBorder.verwrteFlushDecoder.isPlaying {
                receveTurnBorder.wrtngToggleItem()
            }
            receveTurnBorder.sessnRadiusWrap.font = .systemFont(ofSize: 16, weight: .medium)
            receveTurnBorder.sessnRadiusWrap.snp.remakeConstraints { make in
                if receveTurnBorder.pnnFallSubset.lthghWaitHostname.isSelected {
                    make.bottom.equalTo(-20)
                }else{
                    if receveTurnBorder.pnnFallSubset.isMaskShowing{
                        make.bottom.equalTo(-88)
                    }else{
                        make.bottom.equalTo(-20)
                    }
                }
                make.centerX.equalToSuperview()
                make.width.lessThanOrEqualToSuperview().multipliedBy(0.8)
            }
            if nvgteReduceCaret != nil{
                nvgteReduceCaret.alpha = 1
            }
            if repperTalkPurge != nil{
                repperTalkPurge!.alpha = 1
            }
            if cmprseCreateToward != nil{
                cmprseCreateToward!.alpha = 1
            }
            if elementSharedLayout != nil{
                elementSharedLayout!.alpha = 0
            }
            if !lvlProduceRoot.default.selessLargeIncrement(){
                receveTurnBorder.nshftFlattenClick.isHidden = false
            }
            if  !receveTurnBorder.nshftFlattenClick.isHidden{
                if receveTurnBorder.pertnSpeechRule &&
                    !receveTurnBorder.mplementWareWindow &&
                    receveTurnBorder.nshftFlattenClick.alpha == 0{
                    receveTurnBorder.nshftFlattenClick.alpha = 1
                }
                if  !receveTurnBorder.mplementWareWindow {
                    receveTurnBorder.mplementWareWindow = true
                    receveTurnBorder.seqenceViewPlus()
                }
            }
            receveTurnBorder.pnnFallSubset.trndCrazeBand.isSelected = ( receveTurnBorder.verwrteFlushDecoder.playerLayer?.videoGravity == .resizeAspectFill)
        }else{
            receveTurnBorder.sbscrptCycleMuch?.removeFromSuperview()
            receveTurnBorder.sbscrptCycleMuch = nil
            receveTurnBorder.sessnRadiusWrap.font = .systemFont(ofSize: 12, weight: .medium)
            receveTurnBorder.sessnRadiusWrap.snp.remakeConstraints { make in
                make.bottom.equalTo(-29)
                make.centerX.equalToSuperview()
                make.width.lessThanOrEqualToSuperview().multipliedBy(0.8)
            }
            if nvgteReduceCaret != nil{
                nvgteReduceCaret.alpha = 0
            }
            if repperTalkPurge != nil{
                repperTalkPurge!.alpha = 0
            }
            if cmprseCreateToward != nil{
                cmprseCreateToward!.alpha = 0
            }
            if elementSharedLayout != nil{
                elementSharedLayout!.alpha = 1
            }
            receveTurnBorder.nshftFlattenClick.isHidden = true
            receveTurnBorder.verwrteFlushDecoder.playerLayer?.videoGravity = .resizeAspect
            receveTurnBorder.pnnFallSubset.trndCrazeBand.isSelected = false
        }
        self.view.setNeedsUpdateConstraints()
    }
    
}

extension chrgInhibitSuch:UIScrollViewDelegate{
    public func scrollViewDidScroll(_ scrollView: UIScrollView) {
        if scrollView.contentOffset.y < 0 {
            scrollView.contentOffset = .zero
        }
        var ddressSignalMega = [[0, self.rntmeLeftRadius.frame.origin.y],
                     [self.rntmeLeftRadius.frame.origin.y, CGFloat(CGFLOAT_MAX)]]
        if pckgeBorderNote == 1{
            ddressSignalMega = [[0, self.begnBrownPrepend.frame.origin.y],
                      [self.begnBrownPrepend.frame.origin.y, self.rntmeLeftRadius.frame.origin.y],
                      [self.rntmeLeftRadius.frame.origin.y, CGFloat(CGFLOAT_MAX)]]
        }
        let dmensnVeryFloppy = ceil(scrollView.contentOffset.y)
        var extendedTermSymbol = 0
        for nmersMenuAppear in ddressSignalMega{
            let lstBoolIntend = nmersMenuAppear.first
            let prcessSeriesSnapshot = nmersMenuAppear.last
            if let lstBoolIntend = lstBoolIntend,let prcessSeriesSnapshot = prcessSeriesSnapshot{
                if dmensnVeryFloppy >= lstBoolIntend && dmensnVeryFloppy < prcessSeriesSnapshot {
                    chrIndexCard.descendPrintWell(extendedTermSymbol: extendedTermSymbol)
                    break
                }
            }
            extendedTermSymbol += 1
        }
        
    }
}
