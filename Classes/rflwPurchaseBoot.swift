 
 
 
 
 
 

import UIKit
import WWPlate

class rflwPurchaseBoot: BMPlayer {
    override func onOrientationChanged() {
        if !lvlProduceRoot.default.crrgeHostRename {
            super.onOrientationChanged()
        }
    }
}
