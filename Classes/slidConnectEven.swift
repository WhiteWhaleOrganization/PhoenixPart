 
 
 
 
 
 

import UIKit

class slidConnectEven: shrInquiryRoll {
    var cntrlWordShell:UILabel!
    var lederFallParse = 0
    var chrAmountCard:drivTensionFlag?
    override func shdwUnsafeValidate() {
        cntrlWordShell = UILabel()
            .referenceCellPunch(.lbrryRemainPath(trnsfrmUnitUser: 0x5D5D70))
            .cnsleNameCare(.center)
            .ccrcyOnceInvent(.systemFont(ofSize: 16))
            .predctCrossKeep(0)
        contentView.addSubview(cntrlWordShell)
        cntrlWordShell.snp.remakeConstraints { make in
            make.left.equalTo(3)
            make.right.equalTo(-3)
            make.top.equalTo(6)
            make.bottom.equalTo(-6)
        }
    }
    
    override func derelctAlphaCombine() {
        if let chrAmountCard = chrAmountCard{
            cntrlWordShell.textColor = chrAmountCard.nmersRemainWeek ? .lbrryRemainPath(trnsfrmUnitUser: 0x3CDEF4) : .lbrryRemainPath(trnsfrmUnitUser: 0x5D5D70)
            cntrlWordShell.text = chrAmountCard.nknwnFullSkill
        }
    }
}
