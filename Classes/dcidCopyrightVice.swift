 
 
 
 
 
 

import Foundation

extension printExcludeRule {
    
    func seqenceViewPlus(){
        let time = lvlProduceRoot.default.wheresFrameSelector?.pertnManagerTest ?? 5
        DispatchQueue.main.asyncAfter(deadline: .now() + TimeInterval(time)) { [weak self] in
            guard let self = self else {return}
            if !lvlProduceRoot.default.selessLargeIncrement(){
                self.nshftFlattenClick.alpha = 0
                self.ccdentMemoryDisk()
            }
        }
    }
    
    func ccdentMemoryDisk(){
        if pertnSpeechRule{
            let frmUnlikeExpansion = lvlProduceRoot.default.wheresFrameSelector?.plcyBaseRetry ?? 180
            DispatchQueue.main.asyncAfter(deadline: .now() + TimeInterval(frmUnlikeExpansion)) { [weak self] in
                guard let self = self else {return}
                if !lvlProduceRoot.default.selessLargeIncrement(){
                    self.nshftFlattenClick.alpha = 1
                    self.seqenceViewPlus()
                }
            }
        }
    }
}
