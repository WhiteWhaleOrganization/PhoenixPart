 
 
 
 
 
 

import UIKit

class crssEmployeMuch: slidCircuitMega {
    var chrAmountCard:kypdDisappearHard?
    var cntnFlagInput:UIImageView!
    var cmpreBrushHeap:UILabel!
    override func shdwUnsafeValidate() {
        cntnFlagInput = UIImageView().estercIncrementCaret(.scaleAspectFill).descrbeDetailObserve(true).frscrePeriodZero(25).bckspceRedrawBoth(UIColor.lbrryRemainPath(trnsfrmUnitUser: 0x23252A))
        contentView.addSubview(cntnFlagInput)
        cntnFlagInput.snp.remakeConstraints { make in
            make.top.left.right.equalTo(0)
            make.height.equalTo(50)
        }
        
        cmpreBrushHeap = UILabel()
            .predctCrossKeep(2)
            .cnsleNameCare(.center)
            .referenceCellPunch(.white)
            .ccrcyOnceInvent(.systemFont(ofSize: 12))
        contentView.addSubview(cmpreBrushHeap)
        cmpreBrushHeap.snp.remakeConstraints { make in
            make.left.right.equalTo(0)
            make.top.equalTo(cntnFlagInput.snp.bottom).offset(4)
        }
    }
    
    override func derelctAlphaCombine() {
        if let chrAmountCard = chrAmountCard {
            cntnFlagInput.kf.setImage(with: URL(string: chrAmountCard.nknwnKiloPseudo)!)
            cmpreBrushHeap.text = !chrAmountCard.ccdentAcrossBusy.isEmpty ?  chrAmountCard.ccdentAcrossBusy : chrAmountCard.replceLogicWait
        }
    }
}
